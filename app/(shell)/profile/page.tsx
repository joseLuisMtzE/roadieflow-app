import { PageHeader } from "@/components/page-header";

export default function ProfilePage() {
  return (
    <main className="flex flex-1 flex-col pb-8">
      <PageHeader
        title="Perfil"
        description="Tu cuenta y preferencias — próximamente."
      />
    </main>
  );
}
