import { PageHeader } from "@/components/page-header";

export default function EventsPage() {
  return (
    <main className="flex flex-1 flex-col pb-8">
      <PageHeader
        title="Eventos"
        description="Conciertos y fechas — próximamente."
      />
    </main>
  );
}
